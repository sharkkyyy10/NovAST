{
  "name": "novast",
  "version": "1.0.0",
  "description": "High-performance AST skeletonizer local daemon",
  "main": "dist/cli.js",
  "bin": {
    "novast": "dist/cli.js"
  },
  "files": [
    "dist",
    "novast-core"
  ],
  "scripts": {
    "build:core": "cd novast-core && napi build --platform --release",
    "build:cli": "tsup",
    "postbuild": "cp novast-core/*.node ./dist/ || true",
    "build": "npm run build:core && npm run build:cli",
    "postinstall": "npm run build:core && npm run postbuild",
    "dev": "ts-node src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@napi-rs/cli": "^3.2.0",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12"
  }
}

{
  "name": "novast",
  "version": "1.0.0",
  "description": "High-performance AST skeletonizer local daemon",
  "main": "dist/cli.js",
  "bin": {
    "novast": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build:core": "cd novast-core && npm run build",
    "build:cli": "tsup src/cli.ts --format cjs --clean --minify",
    "postbuild": "cp novast-core/*.node ./dist/ || true",
    "build": "npm run build:core && npm run build:cli",
    "dev": "ts-node src/cli.ts"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",
    "@types/node": "^20.12.7"
  }
}
